<% title "Shows" %>

<table>
  <tr>
    <th>Name</th>
    <th>Date</th>
  </tr>
  <% for show in @shows %>
    <tr>
      <td><%=h show.name %></td>
      <td><%=h show.date %></td>
      <td><%= link_to "Show", show %></td>
      <td><%= link_to "Edit", edit_show_path(show) %></td>
      <td><%= link_to "Destroy", show, :confirm => 'Are you sure?', :method => :delete %></td>
    </tr>
  <% end %>
</table>

<p><%= link_to "New Show", new_show_path %></p>
